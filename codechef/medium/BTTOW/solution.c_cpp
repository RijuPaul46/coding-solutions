#include <bits/stdc++.h>
using namespace std;
#define int long long
signed main() {
	// your code goes here
	int n,k;
	cin>>n>>k;
	vector<int>arr(n);
	for(int i=0;i<n;i++)cin>>arr[i];
	int mx=INT_MIN;
	int mn=INT_MAX;
	for(auto &x:arr){
	    int p1=x+k;
	    int p2=max(0,x-k);
	    mx=max(mx,x+k);
	    mn=min(max(0,x-k),mn);
	}

}
